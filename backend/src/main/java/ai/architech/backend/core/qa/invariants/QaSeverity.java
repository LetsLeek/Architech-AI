package ai.architech.backend.core.qa.invariants;

/** {@code registries/finding-codes.yaml}'s own severity scale (AIW-174), ordered weakest to strongest. */
public enum QaSeverity {
	MINOR,
	MAJOR,
	CRITICAL
}

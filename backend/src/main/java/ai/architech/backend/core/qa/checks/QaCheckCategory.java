package ai.architech.backend.core.qa.checks;

/** {@code registries/qa-checks.yaml}'s own {@code category:} column values (AIW-172). */
public enum QaCheckCategory {
	PRECONDITION,
	DETERMINISTIC,
	CONDITIONAL,
	EVIDENCE
}

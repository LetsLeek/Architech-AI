package ai.architech.backend.core.toolexecution;

/** The five capability domains {@code tool-capability-profile.v1.yaml} names - what a Developer tool call belongs to. */
public enum ToolCapability {
	FILESYSTEM,
	PROJECT_EXECUTION,
	DEPENDENCY,
	GIT_INSPECTION,
	BROWSER_RUNTIME
}

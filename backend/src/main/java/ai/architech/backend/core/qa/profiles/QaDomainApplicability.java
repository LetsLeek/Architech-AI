package ai.architech.backend.core.qa.profiles;

/** One domain's declared, unconditional applicability in a frozen profile (AIW-175). */
public enum QaDomainApplicability {
	REQUIRED,
	CONDITIONAL,
	NOT_APPLICABLE
}

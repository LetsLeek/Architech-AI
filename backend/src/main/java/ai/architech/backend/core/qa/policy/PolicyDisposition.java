package ai.architech.backend.core.qa.policy;

/** {@code core/POLICY_AGGREGATOR.md}'s own immutable {@code PolicyEvaluation} outputs (AIW-176). */
public enum PolicyDisposition {
	BLOCK,
	ALLOW,
	ESCALATE
}
